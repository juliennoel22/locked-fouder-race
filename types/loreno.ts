/**
 * Loreno Domain Types & Data Contracts (Hackathon FounderRace 2026)
 * Strict TypeScript types with zero `any` for AI validation and end-to-end stability.
 */

/**
 * Individual flashcard with front (question/concept) and back (answer/definition)
 */
export interface Flashcard {
  id?: string;
  deck_id?: string;
  front: string;
  back: string;
  distractors?: string[];
  order_index?: number;
  created_at?: string;
}

/**
 * Persisted deck entity in Supabase table `decks`
 */
export interface Deck {
  id: string;
  user_id: string;
  title: string;
  subject?: string | null;
  summary?: string | null;
  detailed_content?: string | null;
  initial_quiz_question?: string | null;
  image_url?: string | null;
  image_urls?: string[] | null;
  progress_percent?: number;
  created_at?: string;
  updated_at?: string;
  flashcards?: Flashcard[];
}

/**
 * Structured output generated by Gemini 1.5/3.6 Flash Vision OCR
 */
export interface ScanResult {
  title: string;
  subject: string;
  summary: string;
  detailed_content: string; // Fiche de cours complète en Markdown
  initial_quiz_question: string;
  flashcards: Array<{
    front: string;
    back: string;
    distractors?: string[];
  }>;
}

/**
 * API response contract for `/api/scan`
 */
export interface ScanApiResponse {
  success: boolean;
  deckId?: string;
  data?: ScanResult;
  imageUrl?: string;
  imageUrls?: string[];
  error?: string;
}

/**
 * Client-side notebook item displayed in the dashboard
 */
export interface NotebookItem {
  id: string;
  title: string;
  subject: string;
  emoji: string;
  date: string;
  sourceCount: number;
  deck: ScanResult;
  detailed_content?: string | null;
  progress_percent?: number;
  imageUrl?: string | null;
  imageUrls?: string[] | null;
  isDemo?: boolean;
}

/**
 * Chat message for the AI Exam Tutor
 */
export interface TutorChatMessage {
  role: "user" | "model";
  text: string;
  isStreaming?: boolean;
}
